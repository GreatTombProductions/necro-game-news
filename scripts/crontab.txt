# Necro Game News - Cron Schedule
#
# To install these cron jobs:
#   1. Edit your crontab: crontab -e
#   2. Add the lines below (adjust paths if needed)
#   3. Save and exit
#
# To view current cron jobs:
#   crontab -l
#
# To remove these jobs:
#   crontab -e (then delete the lines)
#
# ============================================
# Necro Game News Automated Deployments
# ============================================

# Daily full deploy at 12:00 PM (noon)
# Checks updates, exports data, generates social content, deploys to Vercel
0 12 * * * /Users/rayheberer/Documents/greattomb/necro-game-news/scripts/cron_deploy.sh full

# Weekly metadata refresh on Fridays at 6:00 PM
# Refreshes game metadata from Steam API before checking updates
0 18 * * 5 /Users/rayheberer/Documents/greattomb/necro-game-news/scripts/cron_deploy.sh refresh

# ============================================
# Notes:
# - Logs are saved to: necro-game-news/logs/cron/
# - Logs older than 30 days are automatically deleted
# - Times are in system local time
# - Cron format: MIN HOUR DAY MONTH WEEKDAY COMMAND
# ============================================
